import React from 'react'
// import PropTypes from 'prop-types'

// $FlowFixMe
import styles from './BoardQuadrant.module.styl'

const BoardQuadrant = () => (
    <div className={styles.board}>
        <h4>Quadrant</h4>
    </div>
)

// BoardQuadrant.propTypes = {
//     name: PropTypes.oneOf([ 'quadrant', 'moscow' ]).isRequired,
// }

export default BoardQuadrant
